"""topology.yaml writer — sole author of the topology file.

Contract (BLUEPRINT §7): every change is a commit in the same DAG under a
`topology/` ref prefix (one store, one audit trail). Entries carry the
evidence (or pre_evidence at birth) that caused them.

P3 scope — stub only in P1.
"""
