"""Lifecycle engine — shadow mode in v1.

Contract (BLUEPRINT §6): humans move tags via `cle tag`; the engine runs
the part-7 state-machine thresholds in shadow and logs what it would have
done. The human/engine divergence log is article-9 material and the
calibration set for going live in v2.

P3 scope — stub only in P1.
"""
