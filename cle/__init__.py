"""CLE — Context Lifecycle Engine.

Reference implementation of the Agent OS series (parts 7-8). Two pillars:
detection (candidate agents emerge from usage) and lifecycle (candidates are
built, replay-validated, trialed, promoted, demoted — evidence-driven, never
prediction-driven). docs/BLUEPRINT.md is the contract for this build.
"""
