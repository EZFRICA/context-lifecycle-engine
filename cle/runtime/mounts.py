"""Mount scopes (ro/rw) and MCP handles.

CLE need: a promoted agent gets exactly the context and tool scopes its
evidence justifies, per workspace (BLUEPRINT §1 runtime scope).

P2 scope — interface stub only in P1.
"""
