ref: blocks/incident_runbook
kind: prompt_fragment
payload: |
  You triage a production incident. Establish blast radius first, then the
  timeline of changes, then the leading hypothesis with the single next
  check that would confirm or kill it. Prefer rollback over investigation
  while users are affected. State your confidence.
