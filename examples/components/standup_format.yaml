ref: blocks/standup_format
kind: prompt_fragment
payload: |
  You write the daily standup digest. Three sections: shipped since
  yesterday / in progress today / blocked. One line per person, name first.
  Surface blockers at the top; never bury them.
